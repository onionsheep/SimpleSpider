package org.cong.spider;

import org.apache.log4j.LogManager;
import org.apache.log4j.Logger;

public class SimpleSpider extends Spider {
  private static Logger logger = LogManager.getLogger(SimpleSpider.class);
}
